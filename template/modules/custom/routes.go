package custom

import "github.com/gin-gonic/gin"

// You can define your route handlers here. In standard development practices,
// this file would contain the logic for registering routes with a Gin engine,
// adding any module-wide logic (middleware, JWT, no-route catches, etc), and
// defining route endpoints

func RegisterRoutes(g *gin.RouterGroup) {
	// Register your custom routes here
	// ...
}
